# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
# Set MOD key to the super key
set $mod Mod4

# Programs keybinding (ordered alphabetically)
set $BROWSER brave
set $EDITOR nvim
set $FILE pcmanfm
set $GEDITOR geany
set $MC kodi
set $PLAYER spotify
set $TERMINAL kitty
set $SS maim
set $VECTOR inkscape
set $VIDEO vlc
set $WALLPAPER nitrogen

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: FiraMono Nerd Font 11

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Use amixer to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec $HOME/.local/bin/volume.sh up
bindsym XF86AudioLowerVolume exec $HOME/.local/bin/volume.sh down
bindsym XF86AudioMute exec $HOME/.local/bin/volume.sh mute
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next 
bindsym XF86MonBrightnessUp exec $HOME/.local/bin/brightness.sh up
bindsym XF86MonBrightnessDown exec $HOME/.local/bin/brightness.sh down

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Take a screenshot
bindsym $mod+control+p exec --no-startup-id exec $SS -B -u $HOME/Pictures/"Screenshot-"$(date +%Y-%m-%d-%H-%M).png

# Launching applications 
bindsym $mod+t exec $TERMINAL
bindsym $mod+b exec $BROWSER
bindsym $mod+o exec $PLAYER
bindsym $mod+f exec $FILE
bindsym $mod+v exec $VIDEO
bindsym $mod+mod1+h exec xterm -hold -e htop
bindsym $mod+mod1+n exec xterm -hold -e neofetch
bindsym $mod+mod1+c exec kitty -e cmatrix
bindsym $mod+m exec $MC
bindsym $mod+i exec $VECTOR
bindsym $mod+n exec $WALLPAPER
bindsym $mod+g exec $GEDITOR

# Killing applications
bindsym $mod+Shift+b exec kill -9 $(pgrep qbittorrent)
bindsym $mod+Shift+u exec kill -9 $(pgrep uget-gtk)
bindsym $mod+Shift+v exec kill -9 $(pgrep vlc)
bindsym $mod+Shift+o exec kill -9 $(pgrep spotify)
bindsym $mod+Shift+m exec kill -9 $(pgrep kodi)
bindsym $mod+q kill

# Shutdown menu
bindsym $mod+Escape exec /usr/share/i3blocks/shutdown_menu

# start rofi (a program launcher)
bindsym $mod+Return exec rofi -nomral-window -modi drun -show drun

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+control+h split h

# split in vertical orientation
bindsym $mod+control+v split v

# enter fullscreen mode for the focused container
bindsym $mod+p fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1:dev"
set $ws2 "2:web"
set $ws3 "3:dnls"
set $ws4 "4:Files"
set $ws5 "5:Music"
set $ws7 "7:Vids"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+7 workspace $ws7

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+7 move container to workspace $ws7

# Open specific programs on specific workspaces
assign [class="kitty"] $ws1
assign [class="Brave-browser"] $ws2
assign [class="Uget-gtk"] $ws3
assign [class="qBittorrent"] $ws3
assign [class="Pcmanfm"] $ws4
for_window [title="Spotify Premium"] move to workspace $ws5
assign [class="Kodi"] $ws7
assign [class="vlc"] $ws7
assign [title="spt"] $ws5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

set $bg_color		#181818
set $border_color	#8AB4F8
set $cb_color		#8AB4F8AA
set $txt_color		#FFFFFF
set $focused_ws		#181818 #181818 #FFFFFF
set $inactive_ws	#181818 #181818 #746CFF
set $urgent_ws		#0059FF #0059FF #FFFFFF
set $inac_bor_cl	#FFFFFF70
set $ind_color		#8AB4F8AA

# Client colors		#border		#background	#text		#indicator	#child_border
client.focused		$border_color	$bg_color	$txt_color	$ind_color	$cb_color
client.unfocused	$inac_bor_cl	$bg_color	$txt_color	$bg_color	$inac_bor_cl
client.focused_inactive $border_color	$bg_color	$txt_color	$bg_color	$cb_color
client.urgent		$border_color	$bg_color	$txt_color	$bg_color	$cb_color

# Define floating clients
# Nitrogen
for_window [class="Nitrogen"] floating enable
for_window [class="Nitrogen"] resize set 1200 700 
for_window [class="Nitrogen"] move position center
# qt5ct
for_window [class="qt5ct"] floating enable
for_window [class="qt5ct"] resize set 1200 700
for_window [class="qt5ct"] move position center
# Pavucontrol
for_window [class="Pavucontrol"] floating enable
for_window [class="Pavucontrol"] resize set 1200 700
for_window [class="Pavucontrol"] move position center
# Blueman-manager
for_window [class="Blueman-manager"] floating enable
for_window [class="Blueman-manager"] resize set 1200 700
for_window [class="Blueman-manager"] move position center
# Network-manager
for_window [class="Nm-connection-editor"] floating enable
for_window [class="Nm-connection-editor"] resize set 1200 700
for_window [class="Nm-connection-editor"] move position center
# Lxappearance
for_window [class="Lxappearance"] floating enable
for_window [class="Lxappearance"] resize set 1200 700
for_window [class="Lxappearance"] move position center
# Kvantum
for_window [class="Kvantum Manager"] floating enable
for_window [class="Kvantum Manager"] resize set 1500 900
for_window [class="Kvantum Manager"] move position center
# Xarchiver
for_window [class="Xarchiver"] floating enable
for_window [class="Xarchiver"] resize set 1200 700
for_window [class="Xarchiver"] move position center
# Arandr
for_window [class="Arandr"] floating enable
for_window [class="Arandr"] resize set 1200 700
for_window [class="Arandr"] move position center
# Dialogs
for_window [window_role="WM_WINDOW_ROLE"] flaoting enable
for_window [window_role="WM_WINDOW_ROLE"] resize set 1200 700

# Define th eborder o fclient and gaps between clients
default_border pixel 1
for_window [class="^.*"] border pixel 1
gaps inner 2
gaps outer 1

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        font pango: FiraMono Nerd Font 10
	colors {
		background $bg_color
		focused_workspace $focused_ws
		inactive_workspace $inactive_ws
		urgent_workspace $urgent_ws
		}
	position top
	status_command i3blocks -c ~/.config/i3blocks/i3blocks.conf
}

# Autostart applications and applets
exec_always --no-startup-id picom --experimental-backend
exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id xfce4-power-manager
exec_always --no-startup-id dunst
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id lxsession
